// import { create } from 'zustand'
// import { IUserSlice,userSlice } from './userSlice' 
// import { chatRoomSlice, chatRoomListSlice } from './chatSlice'
// import { createFishSlice } from './fishSlice'

// export const useRootStore = create<typeof userSlice& typeof chatRoomSlice & chatRoomListSlice & createFishSlice>()((...a) => ({
//   ...userSlice(...a),
//   ...chatRoomSlice(...a),
//   ...chatRoomListSlice(...a),
//   ...createFishSlice(...a),
// }))